/**
 * @file build/link.ts
 * @description provides information about the sync process.
 **/

const link = ( ) => 
{
	console.log( '[yugen-criticals] | symlinks are disabled in this environment.' );
	console.log( '[yugen-criticals] | use vite.local.ts and FOUNDRY_OUT_DIR in .env for live sync.' );
};

link( );
